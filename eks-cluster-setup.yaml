apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: prasad-basic-cluster
  region: ap-south-1
  version: "1.31"

nodeGroups:
  - name: ng-1
    instanceType: t3.small
    desiredCapacity: 2
    volumeSize: 8
    ssh:
      allow: true 
        
    
